ARB_framebuffer_object
	- has attachments of different dimensions (no need to blit across FBOs)

EXT_gpu_shader4
	- has texture2DGrad() in GLSL
	- has GetFragDataLocation() in API

EXT_texture_array
	- no need for special texture sampling, an array element can be a page
