Import( Split( 'root' ) )

### build ######################################################################
build = root.Clone()
out = build.SharedLibrary( '#library/hello', Glob( '*.cpp' ) )
build.SideEffect( Glob( '*.rpo' ), out )
