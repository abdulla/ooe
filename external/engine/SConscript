Import( Split( 'name root' ) )
build = root.Clone()
build.Append( CPPDEFINES = 'OOE_EXTENSION=\\"' + build[ 'SHLIBSUFFIX' ] + '\\"' )
build.Append( LIBS = Split( 'executable general image io lua parallel service' ) )

if name == 'posix':
	build.Append( RPATH = "'$$$${ORIGIN}/../library'" )
elif name == 'darwin':
	build.Append( LINKFLAGS = '-Wl,-rpath,@executable_path/../library' )

out = build.Program( '#binary/engine', Glob( '*.cpp' ) )
build.SideEffect( Glob( '*.rpo' ), out )
build.Clean( out, '#binary/engine.log' )
