Import( Split( 'name root' ) )
build = root.Clone()

if name == 'darwin':
	build.Append( LINKFLAGS = '-Wl,-install_name,@executable_path/library/libio.dylib' )

out = build.SharedLibrary( '#/build/library/libio', Glob( '*.cpp' ) )
build.SideEffect( Glob( '*.rpo' ), out )
